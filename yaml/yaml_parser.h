#ifndef YAML_PARSER_H
#define YAML_PARSER_H

#include <QObject>

class yaml_parser : public QObject
{
    Q_OBJECT
public:
    explicit yaml_parser(QObject *parent = 0);

signals:

public slots:

};

#endif // YAML_PARSER_H
